{
  "name": "buddy-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ devlancer412/buddy-subgraph",
    "create-local": "graph create --node http://localhost:8020/ devlancer412/buddy-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ devlancer412/buddy-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 devlancer412/buddy-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.37.1",
    "@graphprotocol/graph-ts": "0.28.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
